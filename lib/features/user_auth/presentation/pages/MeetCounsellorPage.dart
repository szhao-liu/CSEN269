import 'package:flutter/material.dart';

class MeetCounsellorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Meet with a Counsellor'),
      ),
      body: Center(
        child: Text('This is the Meet with a Counsellor page'),
      ),
    );
  }
}